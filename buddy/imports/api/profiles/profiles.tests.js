/* eslint-env mocha */
import { Meteor } from 'meteor/meteor';

describe('Tasks', () => {
describe('methods', () => {
it('can delete owned task', () => {
console.log("Testing to see if this is running");
	});
	});
});
